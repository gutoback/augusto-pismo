spring.application.name=augusto-pismo
# Disable Hibernate's automatic DDL generation
spring.jpa.hibernate.ddl-auto=none

# Enable Flyway (it's enabled by default if the dependency is present, but explicit is fine)
spring.flyway.enabled=true
# Optional: automatically create a baseline if the database is existing
spring.flyway.baseline-on-migrate=true